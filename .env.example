# Copy to .env and adjust as needed.
# Default league is "Los Socios"; override to point at another league.
LEAGUE_ID=1389689313502961664

# Optional: where to store the on-disk API cache (default: ./.cache)
# SGM_CACHE_DIR=.cache
